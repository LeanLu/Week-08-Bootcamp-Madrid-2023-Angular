import { Component } from '@angular/core';

// Creado con:
//  ng g c home/home --project users2 --flat -t

@Component({
  selector: 'isdi-home',
  template: ` <p>home works!</p> `,
  styleUrls: ['./home.component.scss'],
})
export class HomeComponent {}
